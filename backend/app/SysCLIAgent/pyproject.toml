[project]
name = "sys-cli-agent"
version = "0.1.0"
description = "Resume portfolio agent powered by Strands + Bedrock Nova Pro"
requires-python = ">=3.10"

[project.dependencies]
strands-agents = ">=1.0.0"
strands-agents-tools = ">=0.1.0"
bedrock-agentcore = ">=0.1.0"
